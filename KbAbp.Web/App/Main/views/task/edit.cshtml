<div ng-controller="app.views.task.edit as vm">
    <form id="EditTaskForm" role="form" name="editTaskForm" novalidate>

        <div class="form-group">
            <label for="TaskProject">@L("TaskProject")</label>
            <select ng-model="vm.task.projectID" ng-options="m.id as m.name for m in vm.projects" class="form-control" placeholder="@L("EnterDescriptionHere")" required></select>
        </div>

        <div class="form-group">
            <label for="TaskDescription">@L("TaskDescription")</label>
            <textarea id="TaskDescription" ng-model="vm.task.description" class="form-control" rows="3" placeholder="@L("EnterDescriptionHere")" required></textarea>
        </div>

        <div class="form-group">
            <label for="TaskRemark">@L("TaskRemark")</label>
            <textarea id="TaskRemark" ng-model="vm.task.remark" class="form-control" rows="3" placeholder="@L("EnterRemarkHere")" required></textarea>
        </div>

        <button ng-click="vm.saveTask()" ng-disabled="editTaskForm.$invalid" type="submit" class="btn btn-primary">@L("EditTheTask")</button>

    </form>
</div>